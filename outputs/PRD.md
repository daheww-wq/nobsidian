# PRD — dohohon

> **버전:** 0.3 (확정)
> **작성일:** 2026-06-17
> **상태:** 설계 확정

---

## 1. 제품 개요

### 1.1 배경 및 목적

Obsidian의 유기적 연결성(backlink, 계층 없는 지식 네트워크)과 Notion의 구조적 문서 관리(폴더/페이지 계층, 리치 에디터)를 결합한 웹 기반 메모 앱.
파일은 **GitHub 레포지터리**에 마크다운(.md)으로 저장되어 버전 관리, 이식성, 오프라인 접근성을 동시에 확보한다.

### 1.2 핵심 가치

| 가치      | 설명                                                     |
| --------- | -------------------------------------------------------- |
| 소유권    | 내 GitHub 레포 = 내 파일. 벤더 종속 없음                 |
| 연결성    | `[[백링크]]` 기반 지식 그래프로 노트 간 관계 시각화      |
| 편집 경험 | BlockNote 기반 Notion 스타일 블록 에디터 + / 슬래시 메뉴 |
| 자동화    | AI 요약/태그로 메타데이터 자동 생성                      |

---

## 2. 사용자 정의

### 2.1 주요 사용자 (Primary Persona)

**개인 지식 관리(PKM) 사용자**

- 마크다운에 익숙하고 파일 소유권을 중시하는 개발자/연구자/기획자
- Obsidian은 쓰지만 웹 접근성이 아쉬웠던 사용자
- Notion을 쓰지만 데이터 종속이 불안한 사용자

### 2.2 제외 대상

- 팀 협업 중심 사용자 (v3+ 고려)
- 오프라인 전용 사용자

---

## 3. 범위 정의

### v1 — MVP

| 영역      | 기능                                                                    |
| --------- | ----------------------------------------------------------------------- |
| 인증      | GitHub OAuth 로그인                                                     |
| 저장소    | 사용자 GitHub 레포 선택/연동                                            |
| 사이드바  | 폴더/노트 계층 구조 표시                                                |
| 사이드바  | 폴더/노트 생성, 이름변경, 삭제, 상세설명 확인                           |
| 사이드바  | @dnd-kit/sortable 기반 드래그앤드롭 이동 (순서 변경 + 폴더 이동)        |
| 에디터    | BlockNote WYSIWYG 블록 에디터 (/ 슬래시 메뉴, 인라인 툴바)              |
| 에디터    | 인라인 툴바: 텍스트 선택 시 선택 영역 위에 플로팅 표시 (흰 배경, 5버튼) |
| 저장      | 자동 저장 (2초 디바운스) + 수동 저장 (Cmd/Ctrl+S)                       |
| 수정 이력 | GitHub Commits 기반 diff 뷰어 + 버전 복원                               |

### v2 — AI + 지식 그래프

| 영역      | 기능                                                                    |
| --------- | ----------------------------------------------------------------------- |
| AI        | OpenAI API 기반 노트 요약 생성                                          |
| AI        | 키워드 기반 태그 자동 생성                                              |
| 그래프    | 태그 기반 지식 그래프 시각화 (D3.js)                                    |
| 백링크    | `[[노트명]]` 문법으로 노트 간 연결                                      |
| 백링크    | 상세설명 패널에 연관 노트 목록 표시                                     |
| 우측 패널 | AI 요약/태그/연결노트 표시 패널 — 툴바 토글 버튼으로 슬라이드 열기/닫기 |

---

## 4. 기능 요구사항 요약

### 4.1 인증 및 저장소 연동

- GitHub OAuth 2.0 로그인
- 사용자 레포 목록 조회 및 선택
- 선택된 레포를 `localStorage` + GitHub API로 유지
- 세션 만료 시 자동 재인증 흐름

### 4.2 사이드바 (파일 트리)

- 루트 레벨: 폴더/노트 혼합 표시
- 폴더는 접기/펼치기 가능 (중첩 무제한)
- 우클릭 컨텍스트 메뉴: 우클릭한 아이템 **바로 오른쪽**에 표시 (좌표 자동 반전)
- 드래그앤드롭 (`@dnd-kit/sortable`):
  - 아이템 사이 드롭 → 같은 레벨 순서 변경 (2px 녹색 선 표시)
  - 폴더 위 800ms 호버 → 폴더 열림, 드롭 시 하위로 이동 (녹색 하이라이트)
  - 폴더 드래그 → 하위 파일 전체 경로 일괄 변경
- 상세설명: 마지막 수정일, 파일 크기, 태그(v2), 연결 노트 수(v2)

### 4.3 에디터

- **BlockNote** 기반 WYSIWYG 블록 에디터 (TipTap/ProseMirror 위에 구축)
- 블록 삽입: `/` 슬래시 메뉴 — H2/H3, 체크리스트, 표, 코드블록, 이미지, 백링크(v2)
- **인라인 툴바**: 텍스트 드래그 선택 시 선택 영역 위에 플로팅 표시
  - 구성: Bold / Italic / Strikethrough / InlineCode / Link (5버튼)
  - 스타일: 흰 배경, 회색 테두리, 그림자, 선택 영역 위 중앙 정렬
  - 구현: `FormattingToolbarController`로 커스텀
- 에디터 본문 최대 너비: 960px (충분한 작성 공간 확보)
- v1: WYSIWYG 전용 (마크다운 토글 없음)
- 이미지: 드래그/붙여넣기 업로드 → GitHub 레포 `/assets/images/` 저장

### 4.4 저장

- 자동 저장: 마지막 입력 후 2초 디바운스 → GitHub API commit (`[autosave]`)
- 수동 저장: Cmd/Ctrl+S → GitHub API commit (`[save]`)
- 저장 상태 표시: Saved / Saving... / Unsaved changes / Error

### 4.5 수정 이력

- GitHub Commits를 이력으로 활용 (별도 파일 불필요)
- 에디터 툴바 "이력 보기" 버튼으로 진입
- diff 뷰: 추가(녹색) / 삭제(빨간색) / 컨텍스트(회색)
- 버전 복원: 선택 커밋의 내용으로 현재 파일 덮어쓰기 (새 commit 생성)

### 4.6 AI 기능 (v2)

- 노트 저장 후 백그라운드 OpenAI 호출
- 요약: frontmatter `summary:` 필드에 삽입
- 태그: frontmatter `tags:` 배열에 자동 추가 (기존 태그 보존)
- 사용자 수동 트리거 버튼 제공

### 4.7 지식 그래프 (v2)

- D3.js force-directed graph
- 노드: 노트, 엣지: 백링크 + 공유 태그
- 클릭 시 해당 노트로 이동
- 필터: 태그별, 폴더별

---

## 5. 비기능 요구사항

### 5.1 성능

| 항목             | 목표                               |
| ---------------- | ---------------------------------- |
| 사이드바 로딩    | < 1.5초 (레포 100개 파일 기준)     |
| 에디터 입력 지연 | < 50ms                             |
| 자동 저장 응답   | < 3초 (GitHub API round-trip 포함) |
| 지식 그래프 렌더 | < 2초 (노트 500개 기준)            |

### 5.2 보안

- GitHub OAuth token: `httpOnly` 쿠키 (Vercel 배포)
- API 키(OpenAI): 서버사이드 환경변수만 허용, 클라이언트 노출 금지
- GitHub API 권한: `repo` scope만 요청 (최소 권한 원칙)
- CSRF 방어: state 파라미터 검증

### 5.3 호환성

- 브라우저: Chrome 120+, Firefox 120+, Safari 17+
- 반응형: 데스크탑(1280px+) 우선, 태블릿(768px+) 지원
- 모바일: v2+ 로드맵

---

## 6. 성공 지표 (KPI)

| 지표           | 목표 (3개월)              |
| -------------- | ------------------------- |
| DAU            | 초기 내부 사용자 10명     |
| 노트 생성률    | 사용자당 평균 5개 이상/주 |
| 저장 오류율    | < 0.5%                    |
| AI 태그 수락률 | > 70%                     |

---

## 7. 확정 사항

> 모든 OQ 확정 완료 (2026-06-17)

| #   | 항목              | 결정                                                          |
| --- | ----------------- | ------------------------------------------------------------- |
| 1   | 서버 인프라       | **Vercel** — Next.js 최적화, httpOnly 쿠키 기본 지원          |
| 2   | 에디터 라이브러리 | **BlockNote** — Notion 스타일 블록 에디터, / 슬래시 메뉴 내장 |
| 3   | 이미지 저장       | **레포 내 `/assets/images/`** — 소유권 일관성, 드래그 삽입    |
| 4   | 에디터 모드       | **WYSIWYG 전용 (v1)** — 마크다운 토글은 v2+                   |
| 5   | OpenAI 모델       | **gpt-4o-mini** — 비용 효율 우선                              |
| 6   | 다중 레포         | **단일 레포 고정 (v1)** — 다중은 v3 로드맵                    |
| 7   | 드래그앤드롭      | **@dnd-kit/sortable**                                         |
| 8   | 인라인 툴바       | **슬림 5버튼 + 흰 배경** (Bold/Italic/Strike/Code/Link)       |
| 9   | 수정 이력         | **GitHub Commits 활용** — 별도 백업 파일 불필요               |

### 전제 조건

- 사용자는 GitHub 계정 보유
- 대상 레포는 비공개(private) 또는 공개(public) 모두 지원
- Next.js 15 + TypeScript + Tailwind CSS 기술 스택 확정
