# 기능명세서 — dohohon

> **버전:** 0.4 | **작성일:** 2026-06-17 | **범위:** v1 MVP + v2

---

## 목차

1. [인증 (AUTH)](#1-인증)
2. [레포 연동 (REPO)](#2-레포-연동)
3. [사이드바 / 파일 트리 (SIDEBAR)](#3-사이드바--파일-트리)
4. [검색 (SEARCH) — v1](#4-검색--v1)
5. [에디터 (EDITOR)](#5-에디터)
6. [백링크 기초 (BACKLINK-BASIC) — v1](#6-백링크-기초--v1)
7. [저장 (SAVE)](#7-저장)
8. [수정 이력 (HISTORY)](#8-수정-이력)
9. [AI 기능 (AI) — v2](#9-ai-기능--v2)
10. [지식 그래프 (GRAPH) — v2](#10-지식-그래프--v2)
11. [백링크 고급 (BACKLINK-ADV) — v2](#11-백링크-고급--v2)
12. [보안 요구사항](#12-보안-요구사항)
13. [공통 예외/실패 케이스](#13-공통-예외실패-케이스)

---

## 1. 인증

### 1.1 GitHub OAuth 로그인

**기능 설명**  
사용자가 GitHub OAuth 2.0을 통해 인증하고 액세스 토큰을 발급받아 앱에 로그인한다.

**정상 흐름**

1. 사용자가 `/login` 진입 → "GitHub으로 로그인" 버튼 클릭
2. 서버가 `state` 파라미터(CSRF 방지용 난수) 생성 → 세션에 저장
3. GitHub OAuth URL로 리다이렉트 (`scope=repo,user:email`)
4. GitHub 인증 완료 → `/auth/callback?code=xxx&state=yyy` 수신
5. 서버가 `state` 검증 → code로 access_token 교환 (서버 사이드)
6. access_token을 `httpOnly` Secure 쿠키에 저장
7. 사용자 정보(`/user`) 조회 → DB 또는 세션에 저장
8. 레포 선택 여부 확인 → `/select-repo` 또는 `/workspace`로 이동

**예외/실패 케이스**

| ID   | 케이스         | 원인                            | 처리                                      |
| ---- | -------------- | ------------------------------- | ----------------------------------------- |
| A-01 | state 불일치   | CSRF 공격 또는 세션 만료        | 에러 페이지 표시, 재시도 유도             |
| A-02 | code 교환 실패 | 만료된 코드(10분 내 미사용)     | "인증이 만료되었습니다" 메시지 + 재로그인 |
| A-03 | GitHub API 503 | GitHub 서비스 장애              | GitHub 상태 링크 제공, 재시도 버튼        |
| A-04 | scope 거부     | 사용자가 `repo` scope 거부      | 필요 권한 설명 후 재요청                  |
| A-05 | 쿠키 설정 실패 | Safari ITP / 서드파티 쿠키 차단 | `SameSite=Lax` + 동일 도메인 설정         |
| A-06 | 토큰 만료      | GitHub token 무효화             | 자동 감지 → 재로그인 흐름                 |

**보안 요구사항**

- `state` 파라미터: 최소 32바이트 CSPRNG 값
- access_token: 클라이언트 JS에서 접근 불가 (`httpOnly` 쿠키)
- 쿠키 속성: `Secure; HttpOnly; SameSite=Lax; Path=/`
- 최소 권한: `repo` scope만 요청 (관리자 권한 불필요)
- 토큰 갱신: GitHub OAuth는 refresh_token 없음 → 만료 감지 시 재인증

---

### 1.2 세션 관리

**정상 흐름**

- 쿠키 기반 세션 유지 (브라우저 재시작 시에도 유지)
- API 요청마다 토큰 유효성 자동 검증

**예외 케이스**

| ID   | 케이스              | 처리                                             |
| ---- | ------------------- | ------------------------------------------------ |
| A-07 | 비인증 API 요청     | 401 반환 → 클라이언트 로그인 페이지 이동         |
| A-08 | GitHub token revoke | 다음 API 요청 시 401 감지 → 세션 파기 + 재로그인 |
| A-09 | 멀티 탭 로그아웃    | BroadcastChannel으로 다른 탭에 로그아웃 전파     |

---

## 2. 레포 연동

### 2.1 레포 선택

**기능 설명**  
사용자의 GitHub 레포 목록을 조회하여 노트 저장소로 사용할 레포를 선택한다.

**정상 흐름**

1. `/user/repos?per_page=100&sort=pushed` API 호출
2. 레포 목록 표시 (이름, 공개/비공개, 마지막 수정일)
3. 사용자 선택 → `localStorage`에 `{owner, repo}` 저장
4. 레포 루트에 `.notegraph/config.json` 존재 여부 확인
5. 없으면 초기화 파일 자동 생성 (첫 사용 설정)
6. `/workspace`로 이동

**예외/실패 케이스**

| ID   | 케이스                   | 처리                                                   |
| ---- | ------------------------ | ------------------------------------------------------ |
| R-01 | 레포 0개                 | "새 레포 만들기" 링크 제공 (GitHub로 이동)             |
| R-02 | 레포 100개 초과          | 페이지네이션 또는 검색 기능 제공                       |
| R-03 | 선택 레포 접근 권한 없음 | 403 감지 → "접근 권한이 없는 레포입니다"               |
| R-04 | config.json 생성 실패    | 레포가 비어있지 않은 경우에도 폴백으로 메모리에서 시작 |

**개발 고려사항**

- 레포 목록 캐싱: 5분 TTL (stale-while-revalidate)
- 포크된 레포는 구분 표시 (선택 시 확인 다이얼로그)
- 아카이브된 레포는 읽기 전용 표시

---

### 2.2 파일 트리 로딩

**기능 설명**  
선택된 레포의 전체 파일 트리를 GitHub Git Trees API로 로딩한다.

**정상 흐름**

1. `GET /repos/{owner}/{repo}/git/trees/HEAD?recursive=1`
2. `.md` 파일 및 디렉토리만 필터링
3. 경로 기준으로 계층 구조 파싱
4. 사이드바 FileTree에 렌더링

**예외 케이스**

| ID   | 케이스            | 처리                                                 |
| ---- | ----------------- | ---------------------------------------------------- |
| R-05 | 파일 1,000개 초과 | GitHub API 최대 100,000 파일 지원; 초과 시 경고 표시 |
| R-06 | 빈 레포           | "첫 노트를 만들어보세요!" 빈 상태 UI                 |
| R-07 | 네트워크 오류     | 스켈레톤 UI + 재시도 버튼                            |
| R-08 | 레포 삭제됨       | 404 감지 → 레포 재선택 안내                          |

---

## 3. 사이드바 / 파일 트리

### 3.0 컨텍스트 메뉴 (공통)

**표시 위치**: 우클릭한 아이템 **바로 오른쪽** (`left: calc(100% + 4px); top: 0`)

- 오른쪽 공간 부족 시 → 왼쪽으로 자동 반전
- 하단 공간 부족 시 → 위쪽으로 자동 반전

**메뉴 항목**
| 항목 | 대상 | 단축키 |
|------|------|--------|
| 새 노트 | 폴더/루트 | N |
| 새 폴더 | 폴더/루트 | ⇧N |
| 이름 변경 | 폴더/노트 | F2 |
| 상세 정보 | 폴더/노트 | - |
| 경로 복사 | 폴더/노트 | - |
| 삭제 | 폴더/노트 | Del |

---

### 3.1 폴더 생성

**기능 설명**  
사이드바에서 새 폴더를 생성한다. (GitHub는 빈 폴더를 지원하지 않으므로 `.gitkeep` 파일로 대체)

**정상 흐름**

1. [+폴더] 버튼 또는 컨텍스트 메뉴 → 이름 입력 모달
2. 유효성 검사 통과 → GitHub API로 `{폴더명}/.gitkeep` 파일 생성
3. 파일 트리에 새 폴더 즉시 반영 (Optimistic UI)
4. 새 폴더 자동 선택 및 펼치기

**유효성 검사 규칙**

- 이름 필수, 최소 1자 최대 100자
- 허용 문자: 한글, 영문, 숫자, 하이픈(-), 언더스코어(\_), 공백
- 금지 문자: `/`, `\`, `:`, `*`, `?`, `"`, `<`, `>`, `|`, `.` 시작
- 동일 경로에 같은 이름 중복 금지 (대소문자 구분 없음)

**예외/실패 케이스**

| ID   | 케이스          | 처리                                   |
| ---- | --------------- | -------------------------------------- |
| S-01 | 중복 이름       | "이미 존재하는 이름입니다" 인라인 에러 |
| S-02 | 금지 문자 입력  | 실시간 필터링 또는 인라인 경고         |
| S-03 | GitHub API 실패 | Optimistic UI 롤백 + 에러 토스트       |
| S-04 | 이름 100자 초과 | 입력 차단 + "최대 100자" 안내          |

---

### 3.2 노트 생성

**정상 흐름**

1. [+노트] 또는 컨텍스트 메뉴 → 이름 입력 모달 (`.md` 자동 추가)
2. GitHub API로 frontmatter 포함 초기 파일 생성
3. 트리에 추가 → 해당 노트 자동 열기

**초기 파일 내용**

```markdown
---
id: { uuid-v4 }
title: { 노트명 }
created: { ISO8601 }
updated: { ISO8601 }
tags: []
---
```

**예외 케이스** → S-01~S-04와 동일 + 추가:

| ID   | 케이스                                | 처리                                                |
| ---- | ------------------------------------- | --------------------------------------------------- |
| S-05 | 에디터 미저장 상태에서 다른 노트 생성 | "저장하지 않은 변경사항이 있습니다" 확인 다이얼로그 |

---

### 3.3 이름 변경

**정상 흐름**

1. 컨텍스트 메뉴 → 이름 변경 → 모달
2. 기존 이름 선택된 상태로 표시
3. 확인 → GitHub API: 기존 파일 내용 GET → 새 경로로 PUT → 기존 파일 DELETE
4. v2: 해당 노트를 참조하는 `[[백링크]]` 자동 업데이트

**예외 케이스**

| ID   | 케이스                | 처리                                                     |
| ---- | --------------------- | -------------------------------------------------------- |
| S-06 | 이름 미변경 시 확인   | 변경 없음 감지 → 모달 닫기만 실행                        |
| S-07 | 이름 변경 중 API 실패 | 원본 파일 보존, 생성 시도 파일 삭제, 에러 표시           |
| S-08 | 폴더 이름 변경        | 하위 모든 파일 경로 일괄 업데이트 (GitHub API 병렬 요청) |

---

### 3.4 삭제

**정상 흐름**

1. 컨텍스트 메뉴 → 삭제 → 확인 모달 (복구 불가 경고 포함)
2. GitHub API DELETE 요청
3. 트리에서 제거, 열려있던 에디터 빈 상태로 전환

**예외 케이스**

| ID   | 케이스                      | 처리                                                 |
| ---- | --------------------------- | ---------------------------------------------------- |
| S-09 | 폴더 삭제 시 하위 파일 존재 | "N개의 노트가 함께 삭제됩니다" 경고 포함 확인        |
| S-10 | 삭제 API 실패               | Optimistic UI 롤백 + 에러 토스트                     |
| S-11 | 현재 편집 중인 노트 삭제    | 삭제 완료 후 에디터 초기화 ("노트가 삭제되었습니다") |

---

### 3.5 드래그앤드롭 이동

**라이브러리**: `@dnd-kit/sortable`

**정상 흐름**

1. 왼쪽 핸들(⠿) 클릭 후 드래그 시작 → 원본 아이템 반투명(opacity 40%)
2. **Case A — 아이템 사이 드롭**: 아이템 사이 공간에 2px 녹색 선 표시 → 드롭 시 같은 레벨 순서 변경
3. **Case B — 폴더 위 드롭**: 폴더 위 800ms 호버 → 폴더 자동 열림 + 녹색 하이라이트 → 드롭 시 하위로 이동
4. **Case C — 폴더 자체 드래그**: 폴더 이동 시 하위 파일 전체 경로 일괄 변경 (GitHub API 병렬 요청)
5. Optimistic UI (즉시 트리 반영) → API 실패 시 원상복구 + 에러 토스트

**예외 케이스**

| ID   | 케이스                         | 처리                      |
| ---- | ------------------------------ | ------------------------- |
| S-12 | 폴더를 자기 자신의 하위에 드롭 | 드롭 차단 + 시각적 피드백 |
| S-13 | 동일 위치 드롭                 | 무동작                    |
| S-14 | 이동 중 API 실패               | 롤백 + 에러 토스트        |

---

### 3.6 상세 정보

**표시 정보**

- 파일명, 전체 경로
- 생성일, 마지막 수정일 (Git commit 기준)
- 파일 크기 (KB)
- v2: 백링크 수, 태그 목록, AI 요약

---

## 4. 검색 — v1

### 4.1 실시간 통합 검색

**기능 설명**
사이드바 상단 검색 입력창에서 제목+본문+태그를 실시간으로 검색한다. 노트 수가 증가해도 파일 트리만으로 탐색이 어려워지는 문제를 해결한다.

**정상 흐름**

1. 사이드바 상단 검색 입력창 포커스
2. 파일 트리 영역이 검색 결과 뷰로 전환
3. 빈 쿼리: 최근 수정 노트 목록 표시 (최대 10개)
4. 쿼리 입력 시 실시간 갱신 (디바운스: 150ms)
5. 결과: 매칭 노트 목록 + 매칭된 컨텍스트 스니펫 (본문 50자)
6. 결과 클릭 → 해당 노트 열기 + 검색어 하이라이트

**검색 범위 및 우선순위**
| 우선순위 | 범위 | 가중치 |
|---------|------|--------|
| 1 | 노트 제목 | 높음 |
| 2 | frontmatter 태그 | 중간 |
| 3 | 본문 텍스트 | 낮음 |

**정렬 옵션**

- 관련도순 (기본): 가중치 합산 점수 기준
- 날짜순: 마지막 수정일 내림차순

**검색 구현 방식**

- 클라이언트 인덱스 (`lib/markdown/search.ts`): 레포 로딩 시 제목+태그 인덱싱
- 본문 검색: 파일 로딩 시 점진적 인덱스 추가 (지연 인덱싱)
- 퍼지 매칭: 오타 1~2자 허용 (Levenshtein distance)

**예외 케이스**

| ID    | 케이스                       | 처리                                            |
| ----- | ---------------------------- | ----------------------------------------------- |
| SR-01 | 검색 결과 없음               | "결과 없음" + "새 노트 만들기 '[쿼리명]'" 제안  |
| SR-02 | 인덱스 미구축 (초기 로딩 중) | 스켈레톤 + "인덱싱 중..." 표시                  |
| SR-03 | 본문 미인덱스 노트 검색      | 제목+태그 결과만 표시, 본문 미포함 안내         |
| SR-04 | 검색창 ESC 키                | 쿼리 초기화 + 파일 트리 복귀                    |
| SR-05 | 한글 조합 중 (IME 입력)      | 조합 완료 후 검색 (keydown이 아닌 input 이벤트) |

---

## 5. 에디터

### 5.1 노트 열기

**정상 흐름**

1. 사이드바에서 노트 클릭 → GitHub API로 파일 내용 GET
2. Base64 디코딩 → frontmatter 파싱 → 본문 분리
3. BlockNote 에디터에 콘텐츠 로딩 (마크다운 → Block JSON 변환)
4. URL을 `/workspace/[encodedPath]`로 업데이트

**예외 케이스**

| ID   | 케이스                         | 처리                                                 |
| ---- | ------------------------------ | ---------------------------------------------------- |
| E-01 | 파일 로딩 실패                 | 에러 배너 + 재시도 버튼                              |
| E-02 | 손상된 frontmatter             | frontmatter 무시하고 본문만 표시 + 경고 배너         |
| E-03 | 매우 큰 파일 (>1MB)            | "큰 파일입니다. 로딩에 시간이 걸릴 수 있습니다" 경고 |
| E-04 | 미저장 상태에서 다른 노트 클릭 | "저장하지 않은 변경사항" 확인 다이얼로그             |

---

### 5.2 서식 지원

**에디터: BlockNote** (TipTap/ProseMirror 기반 Notion 스타일 블록 에디터)

**블록 삽입 — / 슬래시 메뉴**

| 타입       | 단축키/트리거               | 비고 |
| ---------- | --------------------------- | ---- |
| 제목 H2    | `/h2` 또는 `## `            |      |
| 제목 H3    | `/h3` 또는 `### `           |      |
| 체크리스트 | `/checklist` 또는 `- [ ] `  |      |
| 표         | `/table`                    |      |
| 코드블록   | `/code` 또는 ` ``` `        |      |
| 이미지     | `/image` 또는 드래그 업로드 |      |
| 백링크     | `/backlink` 또는 `[[`       | v1   |

**인라인 툴바 — 텍스트 선택 시 플로팅 표시**

- **트리거**: 텍스트 드래그 선택
- **위치**: 선택 영역 바로 위, 중앙 정렬 (선택 해제 시 사라짐)
- **스타일**: 흰 배경 (#fff), 회색 테두리, 그림자 (`box-shadow`)
- **버튼 구성 (5개)**:

| 버튼         | 단축키      | 설명       |
| ------------ | ----------- | ---------- |
| **B** Bold   | Cmd+B       | 볼드       |
| _I_ Italic   | Cmd+I       | 이탤릭     |
| ~~S~~ Strike | Cmd+Shift+S | 취소선     |
| `` ` `` Code | Cmd+E       | 인라인코드 |
| 🔗 Link      | Cmd+K       | 링크 삽입  |

- **구현**: `FormattingToolbarController` + 커스텀 `SlimFormattingToolbar`

**예외 케이스**

| ID   | 케이스                                | 처리                                                                  |
| ---- | ------------------------------------- | --------------------------------------------------------------------- |
| E-05 | 표 셀 내 복잡한 블록                  | 인라인 서식만 허용, 중첩 블록 차단                                    |
| E-06 | 이미지 업로드 5MB 초과                | "5MB 이하 이미지만 업로드 가능합니다"                                 |
| E-07 | 지원하지 않는 이미지 형식             | jpg, png, gif, webp, svg만 허용                                       |
| E-08 | BlockNote JSON → 마크다운 직렬화 손실 | 복잡한 중첩 구조(표 내 체크리스트 등)는 단순화하여 저장; v1 허용 범위 |

---

### 5.3 이미지 업로드

**정상 흐름**

1. 드래그앤드롭 또는 붙여넣기
2. 로컬 미리보기 즉시 표시
3. 백그라운드에서 GitHub API로 `/assets/images/{uuid}.{ext}` 업로드
4. 마크다운 이미지 태그로 URL 대체

**예외 케이스**

| ID   | 케이스                            | 처리                                             |
| ---- | --------------------------------- | ------------------------------------------------ |
| E-09 | 업로드 실패                       | 로컬 미리보기 유지 + 에러 표시 + 재시도 버튼     |
| E-10 | 오프라인 상태에서 이미지 붙여넣기 | 로컬 blob URL로 임시 저장, 온라인 복귀 시 업로드 |

---

## 6. 백링크 기초 — v1

### 6.1 `[[notename]]` 인라인 노드

**기능 설명**
에디터 본문에서 `[[노트명]]` 문법으로 다른 노트를 참조한다. BlockNote 커스텀 인라인 노드로 구현한다.

**자동완성**

1. 사용자가 `[[` 입력 시 드롭다운 팝업 표시
2. 입력 계속 시 노트 목록 퍼지 매칭 필터
3. 화살표 키로 이동, Enter 또는 클릭으로 선택 → `[[선택된 노트명]]` 삽입
4. ESC → 드롭다운 닫기, 일반 텍스트로 유지

**렌더링**

- 유효한 링크 (노트 존재): 녹색 + 밑줄 → 클릭 시 해당 노트로 이동
- 깨진 링크 (노트 없음): 회색 + 취소선 표시
- 호버: 연결된 노트 제목 툴팁

**연결 노트 목록 (사이드바 하단)**

- 현재 열린 노트를 `[[참조]]`하는 노트 목록 표시
- `lib/markdown/backlinks.ts` 역방향 인덱스 기반
- 항목 클릭: 해당 노트 열기

**예외 케이스**

| ID    | 케이스                            | 처리                                         |
| ----- | --------------------------------- | -------------------------------------------- |
| BL-01 | 참조 노트가 삭제됨                | 회색+취소선으로 전환, 사이드바 목록에서 제거 |
| BL-02 | 자기 자신 참조 `[[현재노트]]`     | 허용, 별도 시각 처리 없음                    |
| BL-03 | 동일 이름 노트가 다른 폴더에 존재 | 자동완성에 경로 포함 표시 (`폴더명/노트명`)  |
| BL-04 | 노트명에 특수문자 포함            | 마크다운 이스케이프 처리 후 저장             |
| BL-05 | 백링크 100개 이상인 노트          | 사이드바 목록 최대 50개 + "더 보기"          |
| BL-06 | 이름 변경 시 백링크 업데이트      | v2에서 처리 (v1에서는 수동 수정)             |

---

## 7. 저장

### 7.1 자동 저장 (배치 커밋)

**동작**

- 마지막 입력으로부터 2초 후 배치 큐에 추가
- 30초 배치 윈도우: 큐에 쌓인 변경사항을 단일 커밋으로 묶기
- 커밋 메시지: `[autosave] N changes` (N = 큐 항목 수)
- 저장 상태 표시: `Unsaved changes` → `Saving...` → `Saved`

**배치 큐 구현 (`lib/editor/saveQueue.ts`)**

```
입력 발생 → 2초 디바운스 → 큐 push
30초 경과 또는 수동 저장 → 큐 flush
flush: blocknoteToMarkdown() → GitHub API PUT (단일 요청)
```

**예외/실패 케이스**

| ID    | 케이스                               | 처리                                                                  |
| ----- | ------------------------------------ | --------------------------------------------------------------------- |
| SV-01 | SHA 충돌 (다른 기기에서 동시 편집)   | 409 감지 → "외부에서 변경됨" 경고 → 머지 또는 덮어쓰기 선택           |
| SV-02 | 네트워크 오프라인                    | 큐 일시 중단 + `Offline` 표시, 온라인 복귀 시 자동 flush              |
| SV-03 | GitHub API Rate Limit (5,000회/시간) | 배치 전략으로 요청 수 대폭 감소. 소진 시 `Retry-After` 파싱 후 재시도 |
| SV-04 | 429 Too Many Requests                | `Retry-After` 헤더 파싱, 대기 후 재시도                               |
| SV-05 | 저장 중 탭/브라우저 닫기             | `beforeunload` 이벤트로 경고 표시                                     |
| SV-06 | 빈 노트 자동 저장                    | 제목과 본문 모두 비어있으면 저장 스킵                                 |

### 7.2 수동 저장

**동작**

- `Cmd/Ctrl+S` 또는 저장 버튼
- 자동 저장과 동일한 API 흐름
- 이미 Saved 상태라도 버튼은 항상 응답 (확인 피드백 제공)

---

### 7.3 동시 편집 처리 (SHA 충돌)

**흐름**

```
기기A 편집 중 → 기기B에서 동일 파일 저장 → SHA 변경
기기A 저장 시도 → 409 Conflict 수신
→ 현재 내용 + 서버 내용 diff 표시
→ 사용자 선택: [내 버전으로 덮어쓰기] [서버 버전 사용] [수동 머지]
```

---

## 8. 수정 이력

### 8.1 개요

**설계 원칙**: 별도 백업 파일을 만들지 않는다. 모든 저장(자동/수동)이 GitHub commit으로 남으므로 Git 히스토리 자체가 이력이다.

**수정 이력 뷰어 진입**: 에디터 툴바 "이력 보기" 버튼 → 좌우 분할 뷰 (이력 목록 + diff 미리보기)

### 8.2 이력 목록

**표시 정보 (commit당)**

- 저장 시각 (상대적: "방금 전", "3시간 전", 날짜 초과 시 절대)
- 저장 유형: `자동저장` / `수동저장 (Cmd+S)` 배지
- 변경량: +N줄 / -N줄, 파일 크기

**정상 흐름**

1. GitHub API: `GET /repos/{owner}/{repo}/commits?path={filePath}` 호출
2. 최근 50개 커밋 목록 표시
3. 커밋 선택 → 해당 커밋과 이전 커밋 diff 조회
4. diff 미리보기 표시 (추가: 녹색, 삭제: 빨간색, 컨텍스트: 회색)

**예외/실패 케이스**

| ID   | 케이스                      | 처리                                        |
| ---- | --------------------------- | ------------------------------------------- |
| H-01 | 커밋 이력 없음 (첫 저장 전) | "아직 저장 이력이 없습니다" 빈 상태         |
| H-02 | 커밋 50개 초과              | 더 불러오기 버튼 (무한 스크롤)              |
| H-03 | diff 로딩 실패              | 해당 커밋 재시도 버튼                       |
| H-04 | 바이너리 파일 diff          | "바이너리 파일은 diff를 표시할 수 없습니다" |

### 8.3 버전 복원

**정상 흐름**

1. 이력 뷰에서 특정 커밋 선택 → "이 버전으로 복원" 버튼
2. 확인 다이얼로그: "현재 내용도 이력에 저장됩니다"
3. 해당 커밋의 파일 내용 GET → 현재 파일에 PUT (새 commit 생성)
4. 에디터에 복원된 내용 반영 + 성공 토스트

**예외 케이스**

| ID   | 케이스                | 처리                                                |
| ---- | --------------------- | --------------------------------------------------- |
| H-05 | 복원 중 네트워크 오류 | 롤백 없음 (복원 실패, 기존 내용 유지) + 에러 토스트 |
| H-06 | 복원 중 SHA 충돌      | 다른 기기에서 동시 수정 → 409 처리 흐름과 동일      |

### 8.4 커밋 메시지 규칙

모든 저장은 일관된 커밋 메시지 형식을 사용한다:

```
[autosave] 아이디어 정리.md — 2026-06-17 09:51
[save] 아이디어 정리.md — 2026-06-17 09:42
[restore] 아이디어 정리.md — restored to 2026-06-17 09:42
```

---

## 9. AI 기능 — v2

### 9.1 노트 요약

**트리거**

- 노트 저장 후 자동 (백그라운드, 처음 1회)
- 사이드 패널 "요약 갱신" 버튼 수동 트리거

**정상 흐름**

1. 노트 본문 추출 (frontmatter 제외)
2. `/api/ai/summarize` POST 요청
3. 서버에서 OpenAI API 호출 (`gpt-4o-mini`, temperature: 0.3)
4. 응답 → frontmatter `summary:` 필드 업데이트 → GitHub 저장

**프롬프트 설계**

```
다음 노트의 핵심 내용을 2-3문장으로 요약하세요.
불필요한 서두 없이 바로 요약 내용만 작성하세요.
언어: 노트와 동일한 언어 사용
```

**예외 케이스**

| ID    | 케이스                 | 처리                                      |
| ----- | ---------------------- | ----------------------------------------- |
| AI-01 | OpenAI API 키 미설정   | AI 기능 비활성화 표시, 설정 안내          |
| AI-02 | 본문 길이 > 8,000 토큰 | 앞 4,000 + 뒤 4,000 토큰 추출하여 요약    |
| AI-03 | OpenAI 429 Rate Limit  | 큐잉 + exponential backoff                |
| AI-04 | OpenAI 500 오류        | 재시도 2회 후 실패 표시, 수동 재시도 버튼 |
| AI-05 | 빈 노트 요약 요청      | 스킵 + "노트 내용이 없습니다" 안내        |

---

### 9.2 자동 태그 생성

**정상 흐름**

1. 요약과 동시에 `/api/ai/tag` 요청
2. GPT가 키워드 5개 이내 반환 (JSON 배열)
3. 기존 수동 태그 보존 + AI 태그 병합 (중복 제거)
4. frontmatter `tags:` 업데이트

**태그 규칙**

- 소문자 영문 또는 한글
- 공백 없음 (공백은 `-`로 대체)
- 최대 10개 (수동 + AI 합산)

**예외 케이스**

| ID    | 케이스                 | 처리                                              |
| ----- | ---------------------- | ------------------------------------------------- |
| AI-06 | GPT가 잘못된 형식 반환 | JSON 파싱 재시도 or 태그 업데이트 스킵            |
| AI-07 | 태그 10개 초과         | AI 태그 우선순위 낮게 잘라냄, 수동 태그 우선 보존 |

---

## 10. 지식 그래프 — v2

### 10.1 그래프 생성

**노드**: 각 마크다운 노트  
**엣지 종류**:

- 백링크 (`[[노트명]]`): 방향성 있는 엣지
- 공유 태그: 방향성 없는 점선 엣지

**렌더링**

- D3.js force-directed simulation
- 노드 크기: 연결 수 비례
- 노드 색상: 대표 태그별 색상

**예외 케이스**

| ID   | 케이스                | 처리                                       |
| ---- | --------------------- | ------------------------------------------ |
| G-01 | 노트 500개 초과       | WebWorker에서 레이아웃 계산, 점진적 렌더링 |
| G-02 | 고립 노드 (엣지 없음) | 그래프 표시는 하되 별도 영역에 그룹화      |
| G-03 | 순환 백링크 (A→B→A)   | 정상 허용, 시각적으로 화살표로 구분        |

---

## 11. 백링크 고급 — v2

### 11.1 `[[백링크]]` 문법

**자동완성**

- `[[` 입력 시 노트 목록 드롭다운 표시
- 타이핑으로 필터링 (퍼지 검색)
- Enter 또는 클릭으로 선택

**렌더링** (v2 업그레이드)

- 유효한 백링크: 파란색 링크 → 클릭 시 해당 노트로 이동 (v1: 녹색)
- 깨진 백링크 (해당 노트 없음): 빨간색 표시 (v1: 회색+취소선)

**예외 케이스**

| ID     | 케이스                | 처리                                                 |
| ------ | --------------------- | ---------------------------------------------------- |
| ADV-01 | 참조 노트 삭제됨      | 에디터에서 빨간색 표시, 상세 패널에 "깨진 링크" 경고 |
| ADV-02 | 참조 노트 이름 변경됨 | 이름 변경 시 전체 백링크 자동 업데이트               |
| ADV-03 | 자기 자신 참조        | 허용하되 그래프에서 self-loop 표시                   |

---

## 12. 보안 요구사항

### 12.1 인증 보안

| 항목            | 구현 방법                                                                       |
| --------------- | ------------------------------------------------------------------------------- |
| CSRF 방어       | OAuth state 파라미터 + SameSite 쿠키                                            |
| XSS 방어        | BlockNote 내 HTML 콘텐츠 DOMPurify 새니타이징                                   |
| 토큰 보관       | httpOnly 쿠키 (JS 접근 불가). 클라이언트 Zustand에 `accessToken` 절대 저장 금지 |
| 클라이언트 상태 | `isAuthenticated: boolean` + `user` 정보만. 실제 토큰은 서버 전용               |
| API 키 보호     | 서버 환경변수만 사용, .env.local 추가                                           |
| 레포 접근 검증  | 모든 GitHub API 요청 시 owner 검증                                              |

### 12.2 데이터 보안

| 항목                  | 구현 방법                                            |
| --------------------- | ---------------------------------------------------- |
| 전송 보안             | HTTPS 강제 (HSTS 헤더)                               |
| GitHub 파일 쓰기 검증 | SHA 파라미터 필수 포함 (임의 덮어쓰기 방지)          |
| 경로 탈출 방지        | `../` 포함 경로 차단, 화이트리스트 검증              |
| 이미지 업로드 검증    | MIME 타입 + 매직 바이트 검증 (확장자만으로 불충분)   |
| OpenAI 프록시         | 클라이언트가 OpenAI에 직접 요청 불가, 서버 경유 필수 |

### 12.3 입력 유효성 검사

- 모든 사용자 입력: 서버사이드 재검증 (클라이언트 검증 신뢰 금지)
- 파일명: 금지 문자 정규식 필터 (`/[<>:"/\\|?*\x00-\x1F]/`)
- 파일 크기: GitHub API 제한 100MB 이내, 앱 제한 10MB

---

## 13. 공통 예외/실패 케이스

### 13.1 네트워크 오류 처리

| 상황          | 처리 방식                                           |
| ------------- | --------------------------------------------------- |
| 오프라인 감지 | `navigator.onLine` + online/offline 이벤트 감지     |
| 자동 재시도   | exponential backoff (1초, 2초, 4초, 최대 3회)       |
| 재시도 실패   | 토스트 알림 + 수동 재시도 버튼 제공                 |
| Rate Limit    | `X-RateLimit-Remaining` 헤더 모니터링, 소진 전 경고 |

### 13.2 에러 토스트 표준

```
⚠️ [제목] — 간결한 원인 설명
[무시] [재시도]   (timeout: 5초, 에러는 수동 닫기)
```

### 13.3 로딩 상태

- **스켈레톤 UI**: 파일 트리, 에디터 초기 로딩
- **인디케이터**: 저장 중, AI 처리 중
- **프로그레스 바**: 대용량 파일 업로드

### 13.4 브라우저 호환성 예외

| 케이스             | 처리                                    |
| ------------------ | --------------------------------------- |
| IndexedDB 없음     | 오프라인 캐시 미지원 안내               |
| Clipboard API 없음 | 대체 방법 안내                          |
| Drag API 미지원    | 드래그 대신 컨텍스트 메뉴 이동으로 폴백 |

---

## 부록: API Rate Limit 전략

| API             | 제한         | 전략                             |
| --------------- | ------------ | -------------------------------- |
| GitHub REST     | 5,000 req/hr | 파일 트리 캐싱, 불필요 요청 제거 |
| GitHub Contents | 5,000 req/hr | 자동저장 디바운스 2초            |
| OpenAI          | tier별 상이  | 요청 큐 + backoff                |

**예상 요청 수 (활성 사용 1시간 기준)**

- 파일 트리: 1회 (캐시 후 불변)
- 자동 저장: 최대 30회/시간 (2초 디바운스 기준)
- 노트 열기: 20회 (파일별 1회)
- AI 요약: 5회 (저장된 노트 수 기준)
- **합계: ~56회 / 5,000 제한** → 여유 충분
